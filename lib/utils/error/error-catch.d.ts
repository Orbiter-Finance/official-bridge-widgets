/**
 * Check if error is insufficient gas error
 * @param error - Error object to check
 * @returns True if it's insufficient gas error
 */
export declare function isInsufficientGasError(error: unknown): boolean;
